# Use the latest 2.1 version of CircleCI pipeline process engine.
# See: https://circleci.com/docs/2.0/configuration-reference
version: 2.1

parameters:
  cut_rc_release:
    default: false
    type: boolean
  trigger_rc_pr:
    default: false
    type: boolean
  rc_pr_title:
    default: ""
    type: string

# Define a job to be invoked later in a workflow.
# See: https://circleci.com/docs/2.0/configuration-reference/#jobs
jobs:
  cut-rc:
    # Specify the execution environment. You can specify an image from Dockerhub or use one of our Convenience Images from CircleCI's Developer Hub.
    # See: https://circleci.com/docs/2.0/configuration-reference/#docker-machine-macos-windows-executor
    docker:
      - image: cimg/base:stable
    # Add steps to the job
    # See: https://circleci.com/docs/2.0/configuration-reference/#steps
    steps:
      - checkout
      - run:
          name: "Say hello"
          command: "echo Hello, World!"
   open_pull_request_rc:
    macos:
      xcode: &xcode-img 13.4.1
    parameters:
      version_name:
        type: string
        default: ""
    steps:
      - checkout
      - run:
          name: Install dependencies
          command: |
            gem install bundler
            bundle install
      - run:
          name: Build Lokalise FR Translations & Open PR
          command: |
            bundle exec fastlane trigger_lokalise_pr  \
            version_name:"<< parameters.version_name >>"

# Invoke jobs via workflows
# See: https://circleci.com/docs/2.0/configuration-reference/#workflows
workflows:
  create-rc:
    when: << pipeline.parameters.cut_rc_release >>
    jobs:
      - cut-rc
  create_rc_trigger:
    when: << pipeline.parameters.trigger_rc_pr >>
    jobs:
      
      - open_pull_request_rc:
          version_name: << pipeline.parameters.version_name >>
